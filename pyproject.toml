[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nagi-decisions"
version = "0.3.0"
description = "Nagi — typed decisions (choice/score/noul) with probability distributions"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
authors = [{ name = "Nagisanzenin" }]
keywords = ["system-one", "typed-decisions", "calibration", "classification", "agents"]
dependencies = [
  "torch>=2.5,<3",
  "transformers>=5.17,<6",
  "peft>=0.21,<0.22",
  "huggingface_hub>=1,<2",
  "pyyaml>=6.0",
  "numpy",
]

[project.urls]
Homepage = "https://github.com/nagisanzenin/nagi"
Models = "https://huggingface.co/nagisanzeninz"

[tool.setuptools.packages.find]
where = ["src"]
